# Personal Knowledge Agent Dependencies

# Core dependencies
mcp>=1.0.0

# Storage
# SQLite is built into Python, no additional package needed

# Optional dependencies for processors
pypdf>=3.0.0  # For PDF processing (task 3.4)

# Search dependencies
whoosh>=2.7.4  # For full-text search indexing (task 6.1)
scikit-learn>=1.3.0  # For TF-IDF and similarity calculations (task 6.2)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
